{
	"version": 2,
	"buildCommand": "node index.ts",
	"installCommand": "npm install && npx prisma generate",
	"builds": [
		{
			"src": "./index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/"
		}
	]
}
