{
	"version": 2,
	"buildCommand": "npx prisma db push && npx prisma generate && npm start",
	"installCommand": "npm install",
	"builds": [
		{
			"src": "./index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/"
		}
	]
}
